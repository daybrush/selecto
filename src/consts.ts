export const PROPERTIES = [
    "selectableTargets",
    "continueSelect",
    "hitRate",
] as const;

export const EVENTS = [
    "dragStart",
    "selectStart",
    "select",
    "selectEnd",
] as const;

export const METHODS = [
    "click",
    "setSelectedTargets",
] as const;
